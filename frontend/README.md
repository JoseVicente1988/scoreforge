# Scoreforge SaaS (Frontend)

Minimal Next.js (App Router) dashboard for Scoreforge.

## Setup
```bash
cd frontend
npm install
cp .env.local.example .env.local
# edit NEXT_PUBLIC_API_BASE
npm run dev
```

Open: http://localhost:3000
